pipeline {
    agent any

    stages {
        stage('Provision Jenkins Job') {
            steps {
                script {
                    def dslScriptPath = 'jenkins/dls/ansible-zabbix-dsl.groovy'

                    jobDsl(scriptText: readFileFromWorkspace(dslScriptPath))
                }
            }
        }
    }
}
